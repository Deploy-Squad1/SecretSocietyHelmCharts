apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: {{ .Values.frontend.gateway.name }}-to-{{ .Values.core.name }}
spec:
  destination:
    name: {{ .Values.core.name }}
  sources:
    - name: {{ .Values.frontend.gateway.name }}
      action: allow

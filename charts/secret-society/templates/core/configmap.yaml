apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.core.configMap.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.core.configMap.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
{{- with .Values.core.configMap.data }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}

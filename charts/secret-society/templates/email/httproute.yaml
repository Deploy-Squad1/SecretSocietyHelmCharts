apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: {{ .Values.email.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.email.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
spec:
  parentRefs:
  - name: {{ .Values.frontend.gateway.name }}
  rules:
    {{- range .Values.email.httpRoute.rules }}
    {{- with .matches }}
    - matches:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- end }}
      backendRefs:
      - name: {{ .Values.email.name }}
        port: {{ .Values.email.service.port }}

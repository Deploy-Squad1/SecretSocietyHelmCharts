apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.email.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.email.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
spec:
  type: {{ .Values.email.service.type }}
  selector:
    app.kubernetes.io/name: {{ .Values.email.name }}
    {{- include "secret-society.selectorLabels" . | nindent 4 }}
  ports:
  - port: {{ .Values.email.service.port }}
    targetPort: {{ .Values.email.service.port }}

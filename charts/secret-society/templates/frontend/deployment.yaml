apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontend.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.frontend.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.frontend.deployment.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.frontend.name }}
      {{- include "secret-society.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.frontend.name }}
        {{- include "secret-society.labels" . | nindent 8 }}
      annotations:
        consul.hashicorp.com/connect-service: {{ .Values.frontend.name }}
        {{- include "secret-society.consulAnnotations" . | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.frontend.name }}
      containers:
        - name: {{ .Values.frontend.name }}
          image: "{{ .Values.frontend.deployment.image.repository }}:{{ .Values.frontend.deployment.image.tag }}"
          {{- with .Values.frontend.deployment.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
          - containerPort: {{ .Values.frontend.service.port }}
      imagePullSecrets:
      - name: {{ .Values.ecrCredsSecret }}

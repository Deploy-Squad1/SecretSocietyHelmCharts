apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: {{ .Values.frontend.gateway.name }}-to-{{ .Values.voting.name }}
spec:
  destination:
    name: {{ .Values.voting.name }}
  sources:
    - name: {{ .Values.frontend.gateway.name }}
      action: allow

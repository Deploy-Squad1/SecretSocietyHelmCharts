apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: {{ .Values.core.name }}-to-{{ .Values.map.name }}
spec:
  destination:
    name: {{ .Values.map.name }}
  sources:
    - name: {{ .Values.core.name }}
      action: allow

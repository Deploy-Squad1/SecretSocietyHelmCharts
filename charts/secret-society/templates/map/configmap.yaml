apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.map.configMap.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.map.configMap.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
{{- with .Values.map.configMap.data }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}

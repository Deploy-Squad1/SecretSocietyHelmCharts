apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.map.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.map.name }}
    {{- include "secret-society.labels" . | nindent 4 }}
spec:
  type: {{ .Values.map.service.type }}
  selector:
    app.kubernetes.io/name: {{ .Values.map.name }}
    {{- include "secret-society.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.map.service.port }}
      targetPort: {{ .Values.map.service.port }}
